# Production Environment Configuration for PawnFlow Backend
# ⚠️ IMPORTANT: Never commit this file to Git! Update with actual GoDaddy credentials.
# Use this as a template and update with your production values.

# ============ DATABASE CONFIGURATION ============
# Replace with your GoDaddy PostgreSQL database connection string
# Format: postgresql://username:password@host:port/database
# Get these values from GoDaddy cPanel → MySQL/PostgreSQL
DATABASE_URL=postgresql://your_db_user:your_db_password@your_db_host:5432/your_db_name

# ============ SERVER CONFIGURATION ============
PORT=5000
NODE_ENV=production

# ============ JWT CONFIGURATION ============
# ⚠️ CRITICAL: Generate a NEW strong random 32+ character string!
# DO NOT use the default. Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Replace the string below with your generated secret
JWT_SECRET=replace_with_strong_random_32_char_string_generated_above

# ============ CORS CONFIGURATION ============
# Set to your production domain (HTTPS)
# If multiple domains: domain1.com,domain2.com
CORS_ORIGINS=https://your-domain.com

# ============ API CONFIGURATION ============
API_URL=https://your-domain.com

# ============ LOGGING ============
LOG_LEVEL=info

# ============ FEATURE FLAGS ============
ENABLE_PDF_GENERATION=true
ENABLE_CRON_JOBS=true

# ============ PRODUCTION SECURITY ============
# Set to true for production to enforce SSL database connections
DB_SSL_REQUIRED=true
